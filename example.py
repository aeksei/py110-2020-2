import re
import operator

FILENAME = 'example.txt'
oper = {"+": operator.add}

example = re.compile()  # скобочные группы

with
    for line in f:
        result = example.  # парсинг строки
        oper[result[1]](result[0], result[2]) == result[3]

